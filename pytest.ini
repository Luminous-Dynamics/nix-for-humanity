[pytest]
minversion = 7.0
addopts = -ra --strict-markers --ignore=tests/archive
testpaths = tests
pythonpath = src
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    benchmark: marks tests as performance benchmark tests